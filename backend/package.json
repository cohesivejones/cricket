{
  "name": "cricket-scoreboard-backend",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:silent": "vitest run --reporter=dot --silent",
    "test:watch": "vitest",
    "build": "tsc"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.20",
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "vitest": "^3.2.0",
    "wrangler": "^3.28.4"
  }
}
