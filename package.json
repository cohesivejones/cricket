{
  "name": "cricket-scoreboard",
  "version": "1.0.0",
  "description": "In-memory scoreboard app with React TypeScript and Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install": "npm install --prefix frontend && npm install --prefix backend",
    "test": "npm test --prefix frontend && npm run test:silent --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
